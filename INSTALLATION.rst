============
Installation
============

At the command line::

    $ pip install chefsessionlib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv chefsessionlib
    $ pip install chefsessionlib

Or, if you are using pipenv::

    $ pipenv install chefsessionlib

